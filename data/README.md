# Data

This directory documents the generated/anonymized benchmark seed sets and event-stream identifiers used by the released CSV files. The full table and figure reproduction workflow reads the CSV files under `results/`.

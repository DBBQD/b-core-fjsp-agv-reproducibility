# Reproducibility Guide

Install the listed dependencies, run the smoke test, regenerate summary tables, regenerate figures, and verify checksums. The workflow uses relative paths from the repository root.
